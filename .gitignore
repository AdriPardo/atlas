# Host inventory may contain network topology and redacted-but-sensitive paths
inventory/raw/host/

# Local secrets — never commit
.env
**/.env
*.pem
*.key
**/credentials*
**/secrets*

# Dashboard generator (Node toolchain stays on developer machines)
monitoring/dashboard-generator/node_modules/
monitoring/dashboard-generator/dist/
