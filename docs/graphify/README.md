# Notas Graphify (generadas)

Carpeta destino de:

```bash
graphify . --obsidian --obsidian-dir docs/graphify
```

Contenido regenerable — no editar a mano. Abrir el vault padre `docs/` en Obsidian para ver el grafo / canvas.
