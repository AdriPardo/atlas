spring:
  datasource:
    url: jdbc:tc:postgresql:16:///atlas
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
atlas:
  security:
    jwt:
      secret: test-secret-test-secret-test-secret-32b
      expiration-minutes: 60
