.PHONY: install generate test lint format format-check typecheck all

install:
	npm install

generate:
	npm run generate

test:
	npm test

lint:
	npm run lint

format:
	npm run format

format-check:
	npm run format:check

typecheck:
	npm run typecheck

all: generate test lint typecheck
